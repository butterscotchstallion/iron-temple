{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommits"
  ],
  "postUpdateOptions": [
    "gomodTidy",
    "gomodVendor"
  ],
  "pinDigests": true,
  "packageRules": [
    {
      "description": "Group non-major updates per ecosystem to keep PR noise low.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "{{manager}} minor and patch"
    },
    {
      "description": "Vite majors cannot land alone. vitest carries its own vite as a real dependency, not a peer, so `vitest/config`'s defineConfig types the plugins array against THAT vite — on a mismatch every entry in vite.config.ts fails with 'No overload matches this call' (vite 8 x vitest 3 did exactly this). vite-plugin-svelte is coupled the other way: it peers on one exact vite major, and running it a major behind only degrades `pnpm dev`, which no CI gate covers. Group them so a vite major arrives with the versions that match it. The previous rule here pinned vite-plugin-svelte below 7 while vite was 6; vite 8 landed and made it correct, so grouping replaces the pin.",
      "matchPackageNames": [
        "vite",
        "vitest",
        "@vitest/coverage-v8",
        "@sveltejs/vite-plugin-svelte"
      ],
      "matchUpdateTypes": ["major"],
      "groupName": "vite ecosystem major"
    },
    {
      "description": "js-yaml is here only as a pnpm override, to hold orval's copy at or above 4.3.0 for CVE-2026-59869/59870 (see 3d904fe). v5 drops the CommonJS default export, and orval imports it as `import yaml from \"js-yaml\"` — so v5 breaks `pnpm generate:api` outright while clearing nothing the 4.3 line does not already clear. Revisit when orval ships a v5-compatible import.",
      "matchPackageNames": ["js-yaml"],
      "allowedVersions": "<5"
    },
    {
      "description": "TypeScript 7 is installed deliberately as `@typescript/native` (an npm alias for typescript@7), NOT as `typescript`. svelte-check's --tsgo needs BOTH lines present: it runs the Go checker from the alias and still resolves the classic compiler API from `typescript`, which only the 6.x line provides. Bumping this entry to 7 would leave one TypeScript installed instead of two and break `pnpm check`. Hold at 6 until svelte-check can run on 7 alone.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7"
    }
  ]
}
